# Cards (Tunisian Rummy)

- **Smart sort:** before touching `smartOrder` in
  `Sources/Games/Rummy/ViewModels/RummyGameViewModel.swift`, read
  `docs/SMART_SORT_EXAMPLES.md`. It holds Alaadine's ground-truth examples of
  wanted hand layouts. Whenever Alaadine gives a new example of how the smart
  sort should arrange a hand, append it to that file and pin it with a test in
  `Tests/EngineTests/SmartSortTests.swift`.
